import { Meteor } from 'meteor/meteor';

import '../imports/api/answers.js';
import '../imports/api/cards.js';
import '../imports/api/checks.js';
import '../imports/api/checkCards.js';

Meteor.startup(() => {
  // code to run on server at startup
});
